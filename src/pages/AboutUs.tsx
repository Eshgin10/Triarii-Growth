import InnerPageLayout from '@/components/InnerPageLayout';

const AboutUs = () => (
  <InnerPageLayout
    label="ABOUT US"
    title="THE THIRD LINE"
    subtitle="Named after the elite Roman reserve — the Triarii — we are the operators called upon when everything is on the line. Strategy, precision, execution."
  />
);

export default AboutUs;
